import { PortfolioPage } from "@/components/driftline/portfolio-page";

export const metadata = { title: "Portfolio — Driftline" };

export default function WorkPage() {
  return <PortfolioPage />;
}
