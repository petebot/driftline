import { DesignSystemPage } from "@/components/driftline/design-system-page";

export const metadata = { title: "Design system — Driftline" };

export default function DesignSystemRoute() {
  return <DesignSystemPage />;
}
